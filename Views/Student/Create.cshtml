@model ASPCoreWebApiLogin.Student

@{
    ViewData["Title"] = "Create";
}
@Html.AntiForgeryToken()
<head>
   
</head>


<h1>Create</h1>

<h4>Student</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <div id="login-box" class="col-md-12">
            @using (Html.BeginForm("Create", "Student", FormMethod.Post))
            {
                <h3 class="text-center text-info">Student Create</h3>
                <div class="form-group">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <label for="Id" class="text-info"> StudentName</label>
                    @Html.TextBoxFor(M => M.StudentName, new { @class = "form-control", @placeholder = "Id", @required = true, @id = "Id" })
                    <span asp-validation-for="StudentName" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <label for="" class="text-info">Roll Number</label>
                    @Html.TextBoxFor(M => M.Roll_No, new { @class = "form-control", @placeholder = "Enter Roll number", @required = true, @id = "userName" })
                    <span asp-validation-for="Roll_No" class="text-danger"></span>
                </div>
                <div class="form-group" style="width: 120px; background-color: rgba(0,0,255,.1)">

                    @*@Html.DropDownList("departmentList", ViewBag.ListofDepartments as SelectList, "Please select Department")*@
                    @Html.DropDownListFor(model => Model.DptId, ViewBag.ListofDepartments as SelectList, "Please select Department",new  { @class = "form-control" })


                    @*<label asp-for="Department" class="control-label"></label>

        <select class="form-control" asp-for="Department">
            <option>--Select--</option>
            <option value="ECE">ECE</option>
            <option value="EEE">EEE</option>
            <option value="Civil">Civil</option>
        </select>*@
                    @*<span asp-validation-for="Department" class="text-danger"></span>
        <label asp-for=" Department" >Departments</label>
         <select asp-for=" Department" class="form-control ddl"
                 asp-items="@(new SelectList(ViewBag.ListofDepartments,"Value","Text"))"></select>*@
                    
                </div>
                    <div class="col-md-10">
                        Male
                        @Html.RadioButtonFor(model => model.Gender, "Male")
                        Female
                        @Html.RadioButtonFor(model => model.Gender, "Female")
                    </div>
                    <div class="form-group">

                        <input type="submit" name="submit" id="submitbtn" class="btn btn-info btn-md " value="submit" />
                    </div>
                    }
                    <div style="color:green;">@ViewBag.message</div>
                    <div style="color:red;">@ViewBag.message1</div>
                    <div>
                        <a asp-action="Index">Back to List</a>
                    </div>
                </div>
            </div>

    <div>
        <a asp-action="Index">Back to List</a>
    </div>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}


